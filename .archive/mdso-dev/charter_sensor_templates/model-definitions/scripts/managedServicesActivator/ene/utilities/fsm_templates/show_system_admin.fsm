Value name (.+)
Value List ipv4_trusthost ((([0-9]{1,3}\.){3}[0-9]{1,3})\s(([0-9]{1,3}\.){3}[0-9]{1,3}))
Value accprofile (.+)
Value comments (.+)
Value vdom (.+)
Value password (ENC\s.+)

Start
  ^\s{4}edit "${name}"
  ^\s{8}set trusthost[0-9]+? ${ipv4_trusthost}
  ^\s{8}set accprofile "${accprofile}"
  ^\s{8}set comments '${comments}'
  ^\s{8}set vdom "${vdom}"
  ^\s{8}set password ${password}
  ^\s{4}next -> Record
