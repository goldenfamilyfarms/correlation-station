Value model (.+)
Value firmware_major ([0-9])
Value firmware_minor ([0-9])
Value firmware_patch ([0-9])
Value firmware_build ([0-9]+?)
Value serial (.+)
Value hostname (.+)

Start
  ^Version: FortiGate-${model}\sv${firmware_major}\.${firmware_minor}\.${firmware_patch},build${firmware_build},
  ^Serial-Number: ${serial}
  ^Hostname: ${hostname}
