include:
  - /cicd/bump_version_common.yml

bump_version_master:
  stage: bump_version_stage
  tags:
    - bump_version_stage
  image: $ARTIFACTORY_REGISTRY/docker/docker:20.10.8
  extends: .bump_version
  script:
    - echo hello
  artifacts:
    expire_in: 30 mins
    paths:
      - VERSION
  only:
    - master
