apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: correlation-demo-cluster
  region: us-east-1
  version: "1.28"
managedNodeGroups:
  - name: demo-ng
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 2
    ssh:
      allow: false
