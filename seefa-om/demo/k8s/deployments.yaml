apiVersion: v1
kind: Namespace
metadata:
  name: demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mdso-mock
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mdso-mock
  template:
    metadata:
      labels:
        app: mdso-mock
    spec:
      containers:
        - name: mdso-mock
          # placeholder replaced by seefa-om/demo/scripts/push-images.sh
          image: IMAGE_MDSO
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5001
          readinessProbe:
            httpGet:
              path: /tron/api/v1/tokens
              port: 5001
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mdso-mock
  namespace: demo
spec:
  selector:
    app: mdso-mock
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sense-provisioning
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sense-provisioning
  template:
    metadata:
      labels:
        app: sense-provisioning
    spec:
      containers:
        - name: sense-provisioning
          # placeholder replaced by seefa-om/demo/scripts/push-images.sh
          image: IMAGE_PROVISIONING
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5002
---
apiVersion: v1
kind: Service
metadata:
  name: sense-provisioning
  namespace: demo
spec:
  selector:
    app: sense-provisioning
  ports:
    - port: 5002
      targetPort: 5002
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sense-compliance
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sense-compliance
  template:
    metadata:
      labels:
        app: sense-compliance
    spec:
      containers:
        - name: sense-compliance
          # placeholder replaced by seefa-om/demo/scripts/push-images.sh
          image: IMAGE_COMPLIANCE
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5003
---
apiVersion: v1
kind: Service
metadata:
  name: sense-compliance
  namespace: demo
spec:
  selector:
    app: sense-compliance
  ports:
    - port: 5003
      targetPort: 5003
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sense-disconnect
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sense-disconnect
  template:
    metadata:
      labels:
        app: sense-disconnect
    spec:
      containers:
        - name: sense-disconnect
          # placeholder replaced by seefa-om/demo/scripts/push-images.sh
          image: IMAGE_DISCONNECT
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5004
---
apiVersion: v1
kind: Service
metadata:
  name: sense-disconnect
  namespace: demo
spec:
  selector:
    app: sense-disconnect
  ports:
    - port: 5004
      targetPort: 5004
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: correlation-engine
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: correlation-engine
  template:
    metadata:
      labels:
        app: correlation-engine
    spec:
      containers:
        - name: correlation-engine
          # optional placeholder; CI may update this with kubectl set image
          image: IMAGE_CORRELATION_ENGINE
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: CONFIG_ENV
              value: "demo"
---
apiVersion: v1
kind: Service
metadata:
  name: correlation-engine
  namespace: demo
spec:
  selector:
    app: correlation-engine
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
# Redis (for demo, single pod)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7.2-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: demo
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
---
# OTEL Collector (simple)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector
  template:
    metadata:
      labels:
        app: otel-collector
    spec:
      containers:
        - name: otel-collector
          image: otel/opentelemetry-collector-contrib:0.84.0
          command: ["/otelcol-contrib","--config","/conf/otel-collector-config.yaml"]
          volumeMounts:
            - name: otel-config
              mountPath: /conf
          ports:
            - containerPort: 4318 # OTLP HTTP
            - containerPort: 55681
      volumes:
        - name: otel-config
          configMap:
            name: otel-collector-config
---
apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: demo
spec:
  selector:
    app: otel-collector
  ports:
    - port: 4318
      targetPort: 4318
    - port: 55681
      targetPort: 55681
  type: ClusterIP
---
# Expose a public demo ingress (assumes ingress controller installed)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
  namespace: demo
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: net-auto-olly.gff.org
      http:
        paths:
          - path: /correlation-engine
            pathType: Prefix
            backend:
              service:
                name: correlation-engine
                port:
                  number: 8080
          - path: /mdso
            pathType: Prefix
            backend:
              service:
                name: mdso-mock
                port:
                  number: 5001
          - path: /sense/provision
            pathType: Prefix
            backend:
              service:
                name: sense-provisioning
                port:
                  number: 5002
          - path: /sense/compliance
            pathType: Prefix
            backend:
              service:
                name: sense-compliance
                port:
                  number: 5003
          - path: /sense/disconnect
            pathType: Prefix
            backend:
              service:
                name: sense-disconnect
                port:
                  number: 5004
