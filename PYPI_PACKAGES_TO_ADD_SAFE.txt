# OpenTelemetry Packages to Add to requirements_python38.txt
# 
# SAFE VERSION - Excludes packages that already exist
# 
# Instructions:
# 1. Open ./model-definitions/requirements_python38.txt in the PYPI repository
# 2. Add ONLY the packages below at the end of the file
# 3. DO NOT add packages that already exist (see EXISTING PACKAGES section)
# 4. DO NOT modify existing package versions
#
# These packages were generated from pip-compile output with Python 3.6
# All versions are locked to ensure consistency

# ============================================================================
# NEW PACKAGES TO ADD (OpenTelemetry and dependencies)
# ============================================================================

# OpenTelemetry Core
aiocontextvars==0.2.2
backoff==1.11.1
contextvars==2.4
dataclasses==0.8
deprecated==1.3.1
googleapis-common-protos==1.56.3
immutables==0.19
opentelemetry-api==1.12.0
opentelemetry-exporter-otlp-proto-http==1.12.0
opentelemetry-exporter-prometheus==0.33b0
opentelemetry-instrumentation==0.33b0
opentelemetry-instrumentation-requests==0.33b0
opentelemetry-instrumentation-urllib3==0.33b0
opentelemetry-proto==1.12.0
opentelemetry-sdk==1.12.0
opentelemetry-semantic-conventions==0.33b0
opentelemetry-util-http==0.33b0
prometheus-client==0.17.1
protobuf==3.19.6
pyroscope-io==0.8.14
structlog==21.5.0
wrapt==1.16.0

# ============================================================================
# EXISTING PACKAGES - DO NOT ADD THESE (they already exist)
# ============================================================================
# 
# The following packages already exist in requirements_python38.txt.
# DO NOT add them again. Keep the existing versions:
#
# certifi==2021.10.8          (keep existing, don't update to 2025.4.26)
# cffi==1.15.1                (already matches, no change needed)
# charset-normalizer==2.0.7   (keep existing, don't update to 2.0.12)
# idna==2.10                  (keep existing, don't update to 3.10)
# pycparser==2.21             (already matches, no change needed)
# requests==2.26              (keep existing, don't update to 2.27.1)
# typing-extensions==3.10.0.2  (keep existing, don't update to 4.1.1)
# urllib3==1.26.7             (keep existing, don't update to 1.26.20)
#
# ============================================================================
# TESTING RECOMMENDATION
# ============================================================================
#
# After adding these packages and building PYPI:
# 1. Test OpenTelemetry with existing package versions
# 2. Use test_otel_compatibility.py to verify compatibility
# 3. Only update existing packages if OpenTelemetry fails or has issues
# 4. See VERSION_CONFLICT_ANALYSIS.md for detailed recommendations

