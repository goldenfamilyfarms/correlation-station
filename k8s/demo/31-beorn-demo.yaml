---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: beorn-demo
  namespace: demo
  labels:
    app: beorn-demo
    component: sense-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: beorn-demo
  template:
    metadata:
      labels:
        app: beorn-demo
        component: sense-app
    spec:
      containers:
      - name: beorn
        image: <ECR_REGISTRY>/correlation-station-demo/beorn-demo:latest
        ports:
        - containerPort: 8001
          name: http
        env:
        - name: DEMO_MODE
          value: "true"
        - name: SERVICE_NAME
          value: "beorn"
        - name: CORRELATION_GATEWAY_URL
          value: http://correlation-gateway:8081
        - name: ENABLE_OTEL
          value: "true"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://correlation-gateway:8081/api/otlp
        - name: OTEL_SERVICE_NAME
          value: "beorn"
        - name: DEPLOYMENT_ENVIRONMENT
          value: "demo"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 20
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: beorn-demo
  namespace: demo
  labels:
    app: beorn-demo
    component: sense-app
spec:
  selector:
    app: beorn-demo
  ports:
  - port: 8001
    targetPort: 8001
    name: http
  type: LoadBalancer
